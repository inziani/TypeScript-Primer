_a49‍.x([["calculateTax",()=>calculateTax],["default",()=>calculateTaxandSum]]);function calculateTax(price){
  return Number(price) * 1.2;
}

               function calculateTaxandSum(...prices){
  return prices.reduce((total, p) => total += calculateTax(p),0);

}